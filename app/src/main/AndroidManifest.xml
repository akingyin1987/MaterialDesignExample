<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aswifter.material">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".MyApp"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.aswifter.material.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/DrawerLayoutTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".example.RecycleViewActivity"/>
        <activity android:name=".example.EditTextFLActivity"/>
        <activity android:name=".example.CardViewActivity"/>
        <activity
            android:name=".example.AppBarDetailActivity"
            android:theme="@style/DrawerLayoutTheme"/>
        <activity
            android:name=".book.BookDetailActivity"
            android:theme="@style/DrawerLayoutTheme"/>

        <activity android:name=".example.BottomTabActivity"/>

        <activity android:name="com.MainActivity"/>
        <activity android:name="com.ShowImageActivity"/>
        <activity android:name="com.zlcdgroup.photos.SelectPhotoActivity"/>
        <activity android:name="com.zlcdgroup.photos.IndexActivity"/>
        <activity android:name="com.android.camera.CropImage"/>
        <activity
            android:configChanges="orientation|screenSize"
            android:name="me.nereo.multi_image_selector.MultiImageSelectorActivity" />

        <activity android:name="me.nereo.multi_image_selector.MulitImageBrowseActivity" />
    </application>

</manifest>
